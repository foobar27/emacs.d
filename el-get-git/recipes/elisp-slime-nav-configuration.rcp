(:name elisp-slime-nav-configuration
       :type builtin
       :depends (elisp-slime-nav)
       :after (progn
		(require 'elisp-slime-nav)
		(add-hook 'emacs-lisp-mode-hook (lambda () (elisp-slime-nav-mode t)))))
