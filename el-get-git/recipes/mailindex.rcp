(:name mailindex
       :type github
       :description "Integrate Gnus with mailindex"
       :pkgname "marktriggs/mailindex"
       :build (("mkdir" "lisp")
	       ("cp" "contrib/mailindex.el" "lisp/"))
       :load-path "lisp")
